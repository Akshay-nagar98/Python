#How will you compare two lists? 
'''
You can use the sort() method or the sorted() function to sort the lists and then compare them using the == operator.
This method works because if the lists have the same elements, they will be in the same order after sorting.'''
